services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_BASE=http://localhost:8080/api
    ports:
      - "3000:80"
    depends_on:
      - backend
